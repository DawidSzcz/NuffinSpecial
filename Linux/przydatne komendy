#Przeszukiwnie plików pod kątem zawartości

grep -rl "szukana fraza" /ścieżka/do/katalogu(pliku)

-r (w głąb)
-l wypisuje tylko nazwy
-i not casesensitive
-n wupisuje linie

# kopiowanie biblioteki z jakiejs lokalizacji do usrlib

sudo find ../../.. -name "libIL.so" -print0 | xargs -0 sudo cp --target-directory=/usr/lib


